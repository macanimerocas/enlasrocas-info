# Anime en las Rocas — Ratings Data

This repo holds one file, `ratings.csv`, which the site reads directly to build the ratings table at `/calificaciones` on [animerocas.com](https://animerocas.com).
